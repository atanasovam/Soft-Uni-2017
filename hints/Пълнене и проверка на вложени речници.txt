if (singersNames.Contains(name) && songsNames.Contains(song))
                {
                    if (!result.ContainsKey(name))
                    {
                        result[name] = new Dictionary<string, List<string>>();
                    }

                    if (!result[name].ContainsKey(song))
                    {
                        result[name][song] = new List<string>();
                    }

                    if (!result[name][song].Contains(award))
                    {
                        result[name][song].Add(award);
                    }
                }

Първо проверяваме дали дадените "name" и "song" съществуват в предварително подадени колекции. След това проверяваме дали дадения елемент съществува в речника и ако не го добавяме.
