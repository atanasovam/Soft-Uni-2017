RegEx за намиране на всички цели и реални числа с (+ или -).

string pattern = @"[+|-]?\d+(\.\d+)?";





Събиране на мачоветв към променлива.

foreach (Match m in Regex.Matches(demon, pattern))
            {
                damage += decimal.Parse(m.Value);
            }




Ескейпване на RegEx подаден от конзолата.

string key = Console.ReadLine();
            key = Regex.Escape(key);




Вадене на мачовете от различните групи в RegEx-a.

if (match.Success)
                {
                    var left = match.Groups[1].Value;
                    var messgae = match.Groups[2].Value;
                    var right = match.Groups[3].Value;
		...
		}