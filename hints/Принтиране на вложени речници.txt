 foreach (var Name in result.OrderByDescending(x => x.Value.First().Value.Count).ThenBy(x => x.Key))
            {
                foreach (var song in Name.Value)
                {
                    var eventName = song.Key;

                    Console.WriteLine($"{Name.Key}: {song.Value.Count} awards");
                    
                    foreach (var award in song.Value.OrderBy(x => x))
                    {
                        Console.WriteLine($"--{award}");
                    }
                }
            }


Принтиране на речник вложен речник с лист. Сортиран първо по каунта на листа след това по азбучен ред.